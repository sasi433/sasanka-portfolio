# Architecture

## Phase 0 platform foundation

The portfolio is a Next.js 16 App Router application written in TypeScript. React Server Components are the default rendering model. Tailwind CSS is available through the Tailwind PostCSS plugin, while the temporary foundation page uses only a small global stylesheet.

The production target is Cloudflare Workers. `@opennextjs/cloudflare` converts the Next.js build output into `.open-next/worker.js` and `.open-next/assets`; Wrangler then runs or deploys those artifacts in the Workers runtime.

```text
Browser
  -> Cloudflare Worker generated by OpenNext
      -> Next.js App Router application
      -> static assets through the ASSETS binding
```

Local development uses the normal Next.js Node.js server for fast iteration. `initOpenNextCloudflareForDev()` prepares Cloudflare bindings for that development mode. `pnpm preview` performs the production-like OpenNext build and serves it through local `workerd` via Wrangler.

Because OpenNext is not fully compatible with native Windows package symlinks, `pnpm preview:docker` runs the same locked OpenNext preview workflow in a Node.js 24 Linux container. Production deployment remains assigned to Linux-based Cloudflare Workers Builds.

No database, CMS, separate backend, contact provider, analytics service, or Cloudflare resource binding beyond static assets is configured in Phase 0.

## Configuration ownership

- `next.config.ts`: Next.js configuration and local Cloudflare binding initialization.
- `open-next.config.ts`: OpenNext Cloudflare adapter configuration.
- `wrangler.jsonc`: Worker entry point, compatibility settings, static assets, and observability.
- `.dev.vars.example`: empty local variable contract for later phases; it contains no secrets.

The complete target architecture and phase boundaries remain defined in `docs/IMPLEMENTATION_PLAN.md`.
